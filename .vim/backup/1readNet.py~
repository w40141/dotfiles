import csv
import numpy as np
import matplotlib.pyplot as plt

date=[]
count1=[0]
x=[0]
y=[0]

f=open('time_proto_len.csv', 'rb')
dataReader=csv.reader(f)

for row in dataReader:
    if row[1]=='6':
        date.append(int(row[2]))

print date

# l=len(date)
# for i in range(1, 1500):
#     num=count1[i-1]+date.count(i)
#     count1.append(num)
#     y.append(float(count1[i-1])/l)
#     x.append(i)
#
# plt.plot(x, y)
# plt.show()
